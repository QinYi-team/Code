% =========================================================================
%                          Written by Xin Wang and Yi Qin
% =========================================================================
%% 子函数——tanh的导数 the derivative of tanh
function X = dev_tanh(A)

t = tanh(A);                 % 临时参数，计算处tanh函数值，借此计算导数
X = (1 + t) .* (1 - t);      % tanh的导数
